# Fluentd configuration - receive logs via Docker logging driver

<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

# Show everything we receive for debugging
<match **>
  @type stdout
  <format>
    @type json
  </format>
</match>